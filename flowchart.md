Download-----ubuntu.iso 
-------new c file
----7zip library ---- 
extract source,destination(linux os partition)

--- make it as bootable partition

MAKE PART AS EFI USING DISKPART COMMAND ;

ADD ENTRY TO BIOS USING BCDEDIT

SET THE PARTITION AS FIRST PRIORITY ORDER using bcdedit
